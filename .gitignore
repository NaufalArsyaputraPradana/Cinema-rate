# Backend (Django)
backend/venv/
backend/__pycache__/
backend/*.pyc
backend/.env
# Note: we are NOT ignoring db.sqlite3 here just in case you want to push a pre-filled demo database. 
# But for production Vercel, it is highly recommended to use Vercel Postgres instead of SQLite.

# Frontend (React)
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.env
frontend/.env.local
frontend/.env.production
frontend/.env.development

# OS Files
.DS_Store
Thumbs.db

# IDE Files
.vscode/
.idea/
